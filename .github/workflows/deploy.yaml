name: CI-CD WebLogic WAR Deployment

on:
  push:
    branches:
      - main

env:
  APP_NAME: sample
  VERSION: v1.${{ github.run_number }}
  NEXUS_URL: http://192.168.182.166:8081
  NEXUS_REPO: raw-hosted

jobs:

# ==========================================
# CI JOB (master-node / ci-server)
# sample.war already exists in GitHub repo
# ==========================================
  upload:
    name: Upload WAR to Nexus
    runs-on: [self-hosted, ci-server]

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify WAR File Exists
        run: ls -lh sample.war

      - name: Rename WAR with Version
        run: |
          cp sample.war $APP_NAME-$VERSION.war

      - name: Upload WAR to Nexus
        run: |
          curl -u ${{ secrets.NEXUS_USER }}:${{ secrets.NEXUS_PASS }} \
          --upload-file $APP_NAME-$VERSION.war \
          $NEXUS_URL/repository/$NEXUS_REPO/$APP_NAME-$VERSION.war


# ==========================================
# CD JOB (worker-node / cd-server)
# Download from Nexus and deploy to WebLogic
# ==========================================
  deploy:
    name: Deploy WAR to WebLogic
    needs: upload
    runs-on: [self-hosted, cd-server]

    steps:

      - name: Download WAR from Nexus
        run: |
          curl -u ${{ secrets.NEXUS_USER }}:${{ secrets.NEXUS_PASS }} \
          -o app.war \
          $NEXUS_URL/repository/$NEXUS_REPO/$APP_NAME-$VERSION.war

      - name: Stop Old Application
        run: |
          java weblogic.Deployer \
          -adminurl t3://192.168.182.166:7001 \
          -username ${{ secrets.WL_USER }} \
          -password ${{ secrets.WL_PASS }} \
          -undeploy \
          -name sample || true

      - name: Deploy New WAR
        run: |
          java weblogic.Deployer \
          -adminurl t3://192.168.182.166:7001 \
          -username ${{ secrets.WL_USER }} \
          -password ${{ secrets.WL_PASS }} \
          -deploy \
          -name sample \
          -source app.war \
          -targets AdminServer

      - name: Verify Deployment
        run: |
          curl -I http://192.168.182.166:7001/sample || true
